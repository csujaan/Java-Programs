import java.util.Scanner;

class SortWords
{
	public static void main(String [] args)
	{
		int l;
		String str1="";
		char c=' ';
		System.out.println("Enter a word: ");
		Scanner obj=new Scanner(System.in);
		String str=obj.nextLine();
		l=str.length();	
		while(int i<l)
		
		{
			int n=str.indexOf(c);	
			obj1.sort(str.substring(p,n);
		        p=c;
		}
		
			
	
	String sort(String x)
	{
		x=x.toLowerCase();
		len=x.length();
		char temp=' ';
		for (int k=0; k<len; k++)
		{
			if(s